#!/bin/bash

# Write a shell script to count number of shell scripts
# (with sh extension) present in the current directory.

for file in *.sh; do
    ((count++))
done

echo "Total no. of shell scripts in current directory: $count"